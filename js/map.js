export function drawMap(ctx, width, height) {

    // ==========================
    // FONDO DEL BACKROOMS
    // ==========================

    ctx.fillStyle = "#c9bd6b";

    ctx.fillRect(
        0,
        0,
        width,
        height
    );

}
